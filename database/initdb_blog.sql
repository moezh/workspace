CREATE TABLE blog_config (
    name VARCHAR(255) PRIMARY KEY NOT NULL,
    value TEXT NOT NULL
);

INSERT INTO blog_config (name, value)
VALUES 
('domain','com6.cc'),
('subdomain','blog');

CREATE TABLE blog_posts (
    id VARCHAR(255) PRIMARY KEY NOT NULL,
    title VARCHAR(255) NOT NULL,
    summary VARCHAR(1000) NOT NULL,
    content_html TEXT NOT NULL
);

INSERT INTO blog_posts (id, title, summary, content_html)
VALUES 
('install-docker-on-ubuntu','Installing the Latest Version of Docker on Ubuntu 20.04.1 LTS', 'Learn how to install the latest version of Docker on Ubuntu 20.04.1 LTS in a few easy steps. This guide covers all the necessary steps, from updating existing packages to adding the official Docker repository and starting the Docker service. By the end of this post, you will have Docker up and running on your system.', '<p>Installing Docker on Ubuntu 20.04.1 LTS is a simple process. In this guide, we will go through the steps needed to install the latest version of Docker on your system.</p><p>&nbsp;</p><p><b>Step 1: Update existing packages</b></p><p>Start by updating your existing packages by running the following command:</p><div style="border:1px solid #ccc;padding:5px 10px; margin-top:8px; margin-bottom:8px">sudo apt update</div><p>&nbsp;</p><p><b>Step 2: Install dependencies</b></p><p>Next, you will need to install some dependencies that are required for Docker to run. You can do this by running the following command:</p><div style="border:1px solid #ccc;padding:5px 10px; margin-top:8px; margin-bottom:8px">sudo apt install apt-transport-https ca-certificates curl software-properties-common</div><p>&nbsp;</p><p><b>Step 3: Add the Docker repository</b></p><p>Once the dependencies are installed, you can add the official Docker repository to your system by running the following command:</p><div style="border:1px solid #ccc;padding:5px 10px; margin-top:8px; margin-bottom:8px">curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -<br/>sudo add-apt-repository &quot;deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable&quot;</div><p>&nbsp;</p><p><b>Step 4: Install Docker</b></p><p>With the repository added, you can now update your package list and install the latest version of Docker by running the following command:</p><div style="border:1px solid #ccc;padding:5px 10px; margin-top:8px; margin-bottom:8px">sudo apt update<br/>sudo apt install docker-ce</div><p>&nbsp;</p><p><b>Step 5: Start the Docker service</b></p><p>After the installation is complete, you can start the Docker service by running the following command:</p><div style="border:1px solid #ccc;padding:5px 10px; margin-top:8px; margin-bottom:8px">sudo systemctl start docker</div><p>&nbsp;</p><p><b>Step 6: Enable the service</b></p><p>To ensure that Docker starts automatically at boot time, you can enable the service with this command:</p><div style="border:1px solid #ccc;padding:5px 10px; margin-top:8px; margin-bottom:8px">sudo systemctl enable docker</div><p>&nbsp;</p><p><b>Step 7: Add your user to the docker group</b></p><p>Before running any docker command, you need to add your user to the docker group with this command:</p><div style="border:1px solid #ccc;padding:5px 10px; margin-top:8px; margin-bottom:8px">sudo usermod -aG docker $USER</div><p>This will allow you to run docker commands without the need of using &#39;sudo&#39; every time.</p><p>&nbsp;</p><p><b>Check Installation</b></p><p>To check that the installation is working correctly, you can run the following command:</p><div style="border:1px solid #ccc;padding:5px 10px; margin-top:8px; margin-bottom:8px">docker version</div><p>This command will return the version of the Docker installed on your system, and also it will show you the version of the client and the version of the server.</p><p>&nbsp;</p>');